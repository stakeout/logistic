.map
	position relative
	height 510px
	// background-image url(%=static=%general/map.png)
	// background-repeat no-repeat
	// background-position 100% center
	// background-size cover
	z-index 2
	&__overlay
		position absolute
		width 100%
		height 170px
		background black
		opacity 0.3
	&__inner
		position absolute
		left calc(50% - 585px)
		width: 1170px;
		height inherit
		outline 2px solid red
		z-index 1
	&__wrapper
		display -webkit-flex
		display -ms-flex
		display flex
		height inherit
		justify-content flex-end
		align-items: flex-end;
		padding 60px
